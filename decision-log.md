# Decision Log

| ID | UTC time | Decision | Evidence used | Alternatives rejected | Assumption | Owner | Review trigger |
|---|---|---|---|---|---|---|---|
| D-001 | 2026-08-10T09:00:00Z | Single centralized gateway with nftables for all zone segmentation rather than per-zone firewall containers | Hub-and-spoke topology simplifies tc mirror to one sensor point; nftables handles all forwarding policy in one chain | Per-zone firewalls: rejected because sensor would require multiple mirror points and policy would be fragmented across containers | Gateway container has sufficient forwarding capacity for test traffic volumes | Intern | Performance degradation under real production traffic loads |
| D-002 | 2026-08-10T09:30:00Z | Suricata for passive sensor rather than Zeek | Suricata provides signature-based alerting that directly complements nftables deny log prefixes; rule syntax maps naturally to zone-to-zone violation patterns | Zeek: rejected because script-based analysis is better suited for behavioral analytics than segmentation violation alerting | Suricata rule engine can process mirrored traffic from all interfaces without packet loss | Intern | If detection engineering stage requires protocol-level scripting |
| D-003 | 2026-08-10T10:00:00Z | Lightweight Python socket listeners on all hosts rather than real service installations | The scored deliverable tests firewall policy not service behavior; listeners ensure nc connection tests get a response when the path is allowed | Full service installation (OpenSSH, nginx, PostgreSQL): rejected because it adds image build complexity and service configuration without adding firewall-level evidence | Socket listeners are sufficient to differentiate allowed vs denied paths via nc returncode | Intern | If hidden variant tests require application-layer protocol inspection |
| D-004 | 2026-08-10T10:30:00Z | Mirror all gateway interfaces to sensor via tc ingress filters rather than selective mirroring | Full observability ensures no scored path is invisible to the sensor; the brief requires sensor to capture all scored paths | Selective mirroring of only inter-zone interfaces: rejected because it creates observability gaps and requires ongoing maintenance as zones change | tc mirror to eth9 does not introduce measurable latency on test traffic | Intern | If mirror bandwidth exceeds sensor processing capacity |
| D-005 | 2026-08-10T11:00:00Z | Variant-driven config generation from configs/variant.json using Python f-string templates rather than Jinja2 or manual configs | Python script reads second_octet and generates topology, FRR, bootstrap, and nftables configs; changing only variant.json produces a complete new addressing plan | Jinja2 templates: rejected because the template logic is simple enough that f-strings are sufficient and avoid an extra dependency | variant.json contains all addressing information needed to generate all config files | Intern | If config generation needs conditional logic beyond simple substitution |

Every consequential judgment belongs here. A report conclusion without a
decision trail may be treated as unsupported during defense.
